# Claude Code OAuth tokens
# macOS: security find-generic-password -s "Claude Code-credentials" -w | python3 -c "import json,sys; d=json.load(sys.stdin)['claudeAiOauth']; print('ACCESS_TOKEN='+d['accessToken']); print('REFRESH_TOKEN='+d['refreshToken']); print('EXPIRES_AT='+str(d['expiresAt']))"
ACCESS_TOKEN=sk-ant-oat01-...
REFRESH_TOKEN=sk-ant-ort01-...
EXPIRES_AT=0

PORT=8080
